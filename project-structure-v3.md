# 🎬 Auto Shorts Uploader v3 — 프로젝트 구조

## v2 → v3 변경사항

```
❌ 제거: API 키 직접 입력 방식
✅ 변경: OAuth 방식으로 전환 (유저는 버튼만 클릭)
✅ 변경: token_manager → oauth_manager (토큰 자동 갱신)
✅ 변경: api_key 모델 → connection 모델 (access + refresh token)
✅ 추가: 유저 연동 가이드
```

---

## 유저 입장에서 연동 과정

### YouTube 연동

```
[STEP 1] 우리 사이트에서 "YouTube 연결" 버튼 클릭
             ↓
[STEP 2] 구글 로그인 화면이 뜸 (팝업 또는 리다이렉트)
         ┌────────────────────────────────┐
         │  Google에 로그인               │
         │  ┌──────────────────────────┐  │
         │  │ 이메일: ___________      │  │
         │  │ 비밀번호: ___________    │  │
         │  │       [로그인]           │  │
         │  └──────────────────────────┘  │
         └────────────────────────────────┘
             ↓
[STEP 3] "이 앱이 당신의 YouTube에 영상을 올리는 것을 허용하시겠습니까?"
         ┌────────────────────────────────┐
         │  Auto Shorts가 다음을 요청합니다: │
         │                                │
         │  ✓ YouTube 채널에 영상 업로드    │
         │  ✓ 채널 정보 읽기               │
         │                                │
         │    [허용]      [거부]           │
         └────────────────────────────────┘
             ↓
[STEP 4] 허용 클릭 → 자동으로 우리 사이트로 돌아옴
         "YouTube 연결 완료 ✅" 표시

         끝. 이게 전부야.
```

### Instagram 연동

```
[STEP 1] 우리 사이트에서 "Instagram 연결" 버튼 클릭
             ↓
[STEP 2] Facebook 로그인 화면이 뜸
         (인스타 API는 Meta/Facebook을 통해 인증함)
             ↓
[STEP 3] "이 앱이 당신의 Instagram에 콘텐츠를 게시하는 것을 허용하시겠습니까?"
             ↓
[STEP 4] 허용 → 우리 사이트로 돌아옴
         "Instagram 연결 완료 ✅" 표시
```

### 유저 전제조건

```
YouTube:   구글 계정 + YouTube 채널 (누구나 가능)
Instagram: 인스타 비즈니스/크리에이터 계정 + Facebook 페이지 연결 필요
           (개인 계정은 API 업로드 불가)
```

---

## 뒤에서 실제로 일어나는 일 (OAuth 전체 흐름)

```
[유저]                    [우리 서버]                [Google/Meta]
  │                           │                          │
  │── "YouTube 연결" 클릭 ──→│                          │
  │                           │── 인증 URL 생성 ────────→│
  │←── 구글 로그인 화면 ──────│                          │
  │                           │                          │
  │── "허용" 클릭 ──────────────────────────────────────→│
  │                           │                          │
  │                           │←── authorization_code ───│
  │                           │     (일회용 인증 코드)     │
  │                           │                          │
  │                           │── code로 토큰 교환 요청 ─→│
  │                           │                          │
  │                           │←── access_token ─────────│
  │                           │    refresh_token         │
  │                           │                          │
  │                           │── 토큰 암호화 → DB 저장   │
  │                           │                          │
  │←── "연결 완료 ✅" ────────│                          │
```

### 용어 정리

```
authorization_code : 일회용 인증 코드. 토큰으로 교환하면 사라짐.
                     비유: "이 쿠폰을 출입증으로 교환하세요" (1회용)

access_token       : 실제로 API 호출할 때 쓰는 열쇠. 유효기간 짧음 (1시간).
                     비유: "오늘의 출입증"

refresh_token      : access_token 만료 시 새로 발급받는 데 쓰는 열쇠.
                     유효기간 김 (수개월~무기한).
                     비유: "출입증 재발급 카드"

토큰 갱신 흐름:
  access_token 만료 → refresh_token으로 새 access_token 자동 발급
  → 유저는 아무것도 안 해도 됨 (서버가 알아서 처리)
```

---

## 전체 폴더 트리

```
auto-shorts/
│
├── README.md
├── .env.example
├── .env
├── .gitignore
├── requirements.txt
│
├── frontend/
│   ├── index.html                ← 랜딩 / 로그인
│   ├── dashboard.html            ← 영상 업로드 화면
│   ├── settings.html             ← 플랫폼 연결 관리
│   │                               "YouTube 연결" / "Instagram 연결" 버튼
│   │                               연결 상태 표시 (✅ 연결됨 / ❌ 미연결)
│   ├── styles.css
│   └── app.js
│
├── backend/
│   ├── main.py                   ← FastAPI 앱 시작점
│   ├── config.py                 ← 환경변수 관리
│   ├── database.py               ← DB 연결 설정
│   │
│   ├── routers/
│   │   ├── auth.py               ← 회원가입, 로그인, 로그아웃
│   │   ├── oauth.py              ← ⭐ OAuth 연결/콜백/해제
│   │   │                           GET /oauth/youtube → 구글 로그인으로 보냄
│   │   │                           GET /oauth/youtube/callback → 토큰 받아 저장
│   │   │                           DELETE /oauth/youtube → 연결 해제
│   │   ├── publish.py            ← 영상 업로드 요청
│   │   └── logs.py               ← 업로드 이력 조회
│   │
│   ├── services/
│   │   ├── auth_service.py       ← 비밀번호 해싱, JWT 발급
│   │   ├── oauth_service.py      ← ⭐ OAuth URL 생성, 토큰 교환, 갱신
│   │   ├── youtube_service.py    ← YouTube API 호출
│   │   ├── instagram_service.py  ← Instagram API 호출
│   │   └── publish_service.py    ← 업로드 통합 관리
│   │
│   ├── security/
│   │   ├── encryption.py         ← API 토큰 암호화/복호화 (Fernet)
│   │   ├── oauth_manager.py      ← ⭐ 토큰 저장/갱신/만료 관리
│   │   ├── jwt_handler.py        ← 로그인 세션 관리
│   │   └── password.py           ← 비밀번호 해싱 (bcrypt)
│   │
│   ├── models/
│   │   ├── user.py               ← 유저 정보
│   │   ├── connection.py         ← ⭐ 플랫폼 연결 정보 (토큰 저장)
│   │   └── publish_log.py        ← 업로드 이력
│   │
│   ├── utils/
│   │   ├── validators.py         ← 파일 형식/크기 검증
│   │   └── exceptions.py         ← 에러 처리 통일
│   │
│   └── logs/
│       └── app.log               ← 시스템 로그
│
├── database/
│   └── auto_shorts.db            ← SQLite (MVP)
│
└── tests/
    ├── test_auth.py
    ├── test_oauth.py
    ├── test_publish.py
    └── test_encryption.py
```

---

## DB 구조 (v3)

```
┌─ users ──────────────────────────────────────┐
│ id │ email      │ hashed_password │ created  │
├────┼────────────┼─────────────────┼──────────┤
│ 1  │ user@ex.com│ $2b$12$...      │ 2025-02  │
└──────────────────────────────────────────────┘
         │
         │ 1:N
         ▼
┌─ connections ────────────────────────────────────────────────────┐
│ id │ user_id │ platform  │ encrypted_access  │ encrypted_refresh │
│    │         │           │ _token            │ _token            │
├────┼─────────┼───────────┼───────────────────┼───────────────────┤
│ 1  │ 1       │ youtube   │ gAAAAABf...       │ gAAAAABf...       │
│ 2  │ 1       │ instagram │ gAAAAABf...       │ gAAAAABf...       │
├────┴─────────┴───────────┴───────────────────┴───────────────────┤
│ + token_expires_at (만료 시각)                                    │
│ + platform_user_id (유튜브 채널 ID 등)                            │
│ + platform_username (표시용: "내 채널이름")                        │
│ + is_active (연결 상태)                                          │
│ + connected_at / updated_at                                      │
└──────────────────────────────────────────────────────────────────┘
         │
         │ 1:N
         ▼
┌─ publish_logs ───────────────────────────────────────────────────┐
│ id │ user_id │ platform │ status  │ error_message │ published_at │
├────┼─────────┼──────────┼─────────┼───────────────┼──────────────┤
│ 1  │ 1       │ youtube  │ success │ null          │ 2025-02-05   │
│ 2  │ 1       │ instagram│ failed  │ token expired │ 2025-02-05   │
├────┴─────────┴──────────┴─────────┴───────────────┴──────────────┤
│ + video_title (영상 제목)                                         │
│ + platform_video_id (업로드 후 받는 영상 ID)                       │
│ + platform_url (업로드된 영상 URL)                                 │
└──────────────────────────────────────────────────────────────────┘
```

---

## 개발 순서 (v3)

```
Phase 1: 기반 ─────────────────────────────
  ✅ database.py + models/ → DB 세팅
  ✅ security/password.py → 비밀번호 해싱
  ✅ security/jwt_handler.py → 로그인 토큰
  ✅ routers/auth.py → 가입/로그인 API
  ✅ frontend/index.html → 로그인 화면

Phase 2: OAuth 연동 ───────────────────────
  ✅ security/encryption.py → Fernet 암호화
  ✅ security/oauth_manager.py → 토큰 관리
  ✅ services/oauth_service.py → OAuth 흐름
  ✅ routers/oauth.py → 연결/콜백/해제
  ✅ frontend/settings.html → 연결 버튼 화면

Phase 3: YouTube 업로드 ───────────────────
  ✅ services/youtube_service.py → API 호출
  ✅ services/publish_service.py → 업로드 통합
  ✅ routers/publish.py → 업로드 API
  ✅ frontend/dashboard.html → 업로드 화면

Phase 4: 로그 시스템 ──────────────────────
  ✅ models/publish_log.py → 이력 저장
  ✅ routers/logs.py → 이력 조회 API
  ✅ logging 설정

Phase 5: Instagram 추가 ──────────────────
  ✅ services/instagram_service.py
  ✅ oauth_service.py에 인스타 OAuth 추가

Phase 6: 안정화 ───────────────────────────
  ✅ tests/ 작성
  ✅ 에러 처리 보강
```

---

## .env.example (v3)

```env
# 우리 서비스 보안
SECRET_KEY=                    ← JWT 서명용
ENCRYPTION_KEY=                ← 토큰 암호화용 Fernet 키

# DB
DATABASE_URL=sqlite:///./database/auto_shorts.db

# Google OAuth (개발자가 1회 등록)
GOOGLE_CLIENT_ID=              ← Google Cloud Console에서 발급
GOOGLE_CLIENT_SECRET=          ← 위와 같이 발급
GOOGLE_REDIRECT_URI=http://localhost:8000/oauth/youtube/callback

# Meta/Instagram OAuth (개발자가 1회 등록)
META_APP_ID=                   ← Meta for Developers에서 발급
META_APP_SECRET=               ← 위와 같이 발급
META_REDIRECT_URI=http://localhost:8000/oauth/instagram/callback

# 로그
LOG_LEVEL=INFO
```

---

## 개발자(너)가 1회만 하면 되는 사전 준비

### Google (YouTube용)
```
1. console.cloud.google.com 접속
2. 새 프로젝트 생성
3. YouTube Data API v3 활성화
4. OAuth 2.0 클라이언트 ID 생성
5. Client ID + Secret을 .env에 저장
```

### Meta (Instagram용)
```
1. developers.facebook.com 접속
2. 새 앱 생성 (비즈니스 유형)
3. Instagram Graph API 추가
4. instagram_content_publish 권한 요청
5. 앱 심사 제출 (이 과정이 오래 걸림)
6. 승인 후 App ID + Secret을 .env에 저장
```

> ⚠️ Meta 앱 심사는 수일~수주 걸릴 수 있음.
> 그래서 YouTube 먼저 완성하고 Instagram은 Phase 5로 뒤로 뺌.
