# ============================================
# Reelshorter 환경변수
# ============================================

# 데이터베이스 (PostgreSQL)
# 로컬: docker run -e POSTGRES_PASSWORD=pw -p 5432:5432 postgres
# Vercel: Vercel Dashboard > Storage > Postgres 에서 자동 생성
DATABASE_URL="postgresql://user:password@localhost:5432/reelshorter"

# NextAuth
# 생성: openssl rand -base64 32
NEXTAUTH_SECRET="openssl-rand-base64-32-결과를-여기에"
# 로컬: http://localhost:3000 / 프로덕션: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# 암호화 키 (OAuth 토큰 암호화용)
# 생성: openssl rand -base64 32
ENCRYPTION_KEY="openssl-rand-base64-32-결과를-여기에"

# Google OAuth (YouTube)
# GCP Console > APIs & Services > Credentials 에서 생성
# 로컬 개발 시: docs/google_client_secret*.json 파일이 있으면 자동 사용
# 프로덕션 시: 아래 환경변수 필수 설정
GOOGLE_CLIENT_ID="xxx.apps.googleusercontent.com"
GOOGLE_CLIENT_SECRET="GOCSPX-xxx"

# Meta OAuth (Instagram) - Phase 6
# META_APP_ID="xxx"
# META_APP_SECRET="xxx"
